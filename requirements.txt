# Top-level convenience pointer. The canonical pinned list is in
# experiments/requirements.txt (torch, torchvision, numpy, scipy, matplotlib,
# pyyaml, pytest). The supplementary review-experiment scripts use the same deps.
-r experiments/requirements.txt
